# What the Owner decided, what the independent reviewer decided

Every plan here was reviewed twice: once by the Question Owner that developed it with the dataset planner, and once by a reviewer on a different vendor that never saw that conversation. This page is both decisions for every family that reached that stage, so "independently reviewed" is something you can check rather than something you are told.

In this run the two agreed on 5 of 5 families.

Agreement is not the same as a rubber stamp and it is not proof of one either. Read the reviewer's own reasoning below and decide which it looks like; that is why it is printed rather than summarised.

## Persistent versus sequential population dynamics during evidence accumulation

- **Question Owner** — Accepted (`openai` / `gpt-5.6-terra`)
- **Independent reviewer** — Accepted (`anthropic` / `claude-sonnet-5`)

**The reviewer's reasoning.** Round 2 substantively resolves both round-0 scientific blockers. The representational-form variant now jointly estimates a protected signed-evidence block E with a separate nuisance block N, explicitly forbids residualizing/regressing out signed contrast before fitting E, requires an overlap/collinearity diagnostic, and labels non-identifiable populations 'unresolved' rather than forcing a classification — this makes the evidence-conditioned estimand identifiable while still separating sensory/commitment/movement structure. The behavioral-prediction variant now freezes a fold-safe, prediction-time availability registry before any endpoint fitting, restricts primary baselines to stimulus-side covariates knowable before a fixed pre-movement time, and excludes all response-linked and movement-linked fields from both primary choice and reaction-time models, with any early-movement feature relegated to a clearly separated, non-primary sensitivity analysis. Both revisions directly target the identified integrity concerns without drifting the protected scientific questions or merging representational-form and behavioral-relevance claims. The remaining unresolved items (structural eligibility thresholds, temporal-scale grid, movement-missingness and RT validity policy) are bounded, outcome-independent implementation choices appropriate to lock before execution and do not undermine the credibility of the planning dossier. No new scientific blocker is identified.

**Per criterion:**

- *intent preservation* — **pass**. Both variants preserve the protected distinction between representational form (persistent/sequential/mixed/scale-dependent classification) and behavioral relevance (incremental predictive contribution), matching the family's forbidden_semantic_merges and shared theoretical tension.
- *scientific value* — **pass**. The joint E/N identifiability design and the fold-safe availability registry each represent a genuine methodological advance over the cited comparators (regional tuning vs. sequential mechanisms; heterogeneous accumulators) and directly adjudicate the stated tensions.
- *dataset grounding* — **pass**. Required variables (spike stores, trial timing, contrast, choice, response/movement fields, behavior features) are documented in the schema and metadata evidence views and are used consistently with their documented keys and grains.
- *overclaim boundaries* — **pass**. Claim ceilings (descriptive; predictive) are appropriately bounded, interpretation_limits explicitly disclaim causal or mechanistic inference, and scope is limited to eligible recorded populations in the documented task.
- *competing explanations* — **pass**. Each variant enumerates plausible alternative explanations (sensory-locked latency, commitment/movement preparation, unequal unit quality/timescale, hybrid pooling, leakage) and designs diagnostics or exclusions targeting them.
- *controls* — **pass**. Both variants specify concrete positive controls (synthetic recovery of injected signals) and negative controls (label permutation, post-movement exclusionary windows, leakage demonstrations) appropriate to their estimands.
- *material revision risk* — **pass**. The revision changes analysis mechanics substantially (joint E/N estimation replacing residualization; explicit availability registry) but these changes are direct, faithful responses to the two previously identified blockers and do not alter the protected question, estimands, or scope in an unauthorized way.
- *sibling separation* — **pass**. Variant 01 remains a descriptive, within-task representational-form classification and variant 02 remains a predictive, held-out incremental-prediction comparison; each explicitly disclaims performing the other's analysis.
- *prior issue resolution* — **pass**. Both round-0 scientific blockers (evidence-preserving nuisance separation; fold-safe response-time/movement availability boundary) are now concretely operationalized in the analysis_strategy with explicit prohibitions, diagnostics, and fallback 'unresolved' or sensitivity-analysis handling rather than being merely asserted as resolved.

## Decision-related population structure after richer movement accounting

- **Question Owner** — Accepted (`openai` / `gpt-5.6-terra`)
- **Independent reviewer** — Accepted (`anthropic` / `claude-sonnet-5`)

**The reviewer's reasoning.** Both variants preserve their assigned, opposite roles for movement (competing explanation vs. target of interest) as required by the family's forbidden-semantic-merge guardrails, and each presents an evidence-grounded, associational plan with explicit boundary definitions, matched-flexibility comparisons, session/insertion-grouped validation, negative and positive controls, and honest interpretation limits (no causal decision-representation claim). Dataset grounding is adequate for planning: documented trial/behavior/ephys schemas support the proposed joins and constructs, with future shard-decoding and alignment work correctly flagged as required executor skills rather than treated as already solved. The two Owner-classified issues are properly scoped as pre-execution locks (timing-margin/eligibility rule; feature-representation and RT-transformation prespecification) that must be fixed before outcome evaluation but do not undermine the current planning product's scientific credibility. No scientific blocker or hard-boundary issue is present at this initial round.

**Per criterion:**

- *intent preservation* — **pass**. Variant-01 assigns behavior the competing-explanation role for a residual decision signal; variant-02 assigns behavior the target-of-interest role and asks whether it improves neural/outcome prediction. This matches the family's target-preservation vs. reinterpretation design and does not merge the two forbidden semantic roles.
- *scientific value* — **pass**. Both plans engage the substantive tension of whether decision-related activity survives richer embodied controls, with nested held-out comparisons and reproducibility checks that would meaningfully constrain the competing accounts.
- *dataset grounding* — **pass**. Required variables and joins (eid/trial_id, pid/cluster_id, DLC/pupil/wheel streams, spike shards) are documented in the cited schema and inspection evidence; shard decoding is correctly deferred to a future executor skill rather than assumed.
- *overclaim control* — **pass**. Claim ceiling is associational in both variants, with explicit interpretation_limits stating that a surviving increment or improvement cannot establish a causal decision representation.
- *competing explanations* — **pass**. Both plans enumerate plausible alternative explanations (clock misalignment, unmeasured behavior/arousal, task locking, latent-state proxying, unequal flexibility) and design comparisons to address them.
- *controls adequacy* — **pass**. Negative controls (label permutation, post-boundary/time-shifted features as leakage diagnostics) and positive controls (anchor-ordering and channel-recovery checks) are specified alongside grouped cross-validation to avoid pseudo-replication.
- *material revision risk* — **not_applicable**. This is round zero with no prior review history or revision to assess.
- *sibling separation* — **pass**. The two variants maintain distinct, non-overlapping interpretive roles for movement consistent with the family's allowed target_contrast axis and forbidden semantic merges.
- *prior issue resolution* — **not_applicable**. Issue ledger and prior review history are empty at this initial round.

## Functional geometry of shared neural variability

- **Question Owner** — Accepted (`openai` / `gpt-5.6-terra`)
- **Independent reviewer** — Accepted (`anthropic` / `claude-sonnet-5`)

**The reviewer's reasoning.** The plan operationalizes the protected decision-formation versus movement-execution contrast using temporally separated, independently estimated axes, cross-fitted to avoid leakage, and requires incremental out-of-sample prediction of both choice and reaction time beyond sensory alignment, movement covariates, and total shared-variability magnitude. Comparable alignment is explicitly treated as mixed rather than silently reinterpreted as decision-specific, matching the family's forbidden-merge constraint. Claims are capped at associational, interpretation limits are honest about latent-decision and motor-confound uncertainty, and positive/negative controls plus diagnostics support falsifiability. The two Owner issues concern a pre-movement window/buffer specification and a movement-eligibility/missing-data policy; both are execution-stage parameter choices that do not undermine the plan's current scientific logic and are correctly pre-execution locks rather than blockers.

**Per criterion:**

- *intent preservation* — **pass**. The decision-versus-movement separation and non-unique treatment of comparable alignment directly preserve the family's protected contrast and forbidden semantic merges.
- *scientific value* — **pass**. The plan targets a genuine discriminating question (incremental prediction of choice and RT) rather than mere correlation magnitude, adding value beyond prior sensory/action-plan alignment work.
- *dataset grounding* — **pass**. Required variables (trial keys, timing fields, spikes, wheel/DLC features) are documented in the schema and build-metadata evidence views at sufficient scale, with acknowledged coverage caveats for wheel/DLC.
- *overclaim control* — **pass**. Claim ceiling is associational and interpretation limits explicitly disclaim causal decision-formation inference and acknowledge residual confounding.
- *alternative explanations* — **pass**. Sensory-axis, movement-preparation, reliability/dimensionality mismatch, arousal/history state, and population-heterogeneity alternatives are enumerated and addressed via controls.
- *controls* — **pass**. Positive controls (sensory-axis and movement-axis recovery), negative controls (label permutation, random-direction projection, temporally shifted movement features), and nested nuisance nested predictive models are all specified.
- *material revision* — **not_applicable**. This is review round zero with no prior plan version to compare against.
- *sibling separation* — **pass**. The plan explicitly distinguishes itself from the sensory-alignment sibling by requiring decision-aligned incremental prediction beyond sensory alignment, consistent with the allowed variant axes.
- *prior issue resolution* — **not_applicable**. No prior review history or issue ledger entries exist at round zero.

## Functional forms of mixed selectivity in decision populations

- **Question Owner** — Accepted (`openai` / `gpt-5.6-terra`)
- **Independent reviewer** — Accepted (`anthropic` / `claude-sonnet-5`)

**The reviewer's reasoning.** The plan operationalizes the functional variant faithfully: it pits prespecified additive and nonlinear representation families against each other without assigning class from the success outcome, matches capacity/training budget, and treats held-out decision-success prediction and cross-condition readout as separate, jointly reported endpoints (aligned/independent/traded-off), which is exactly the forbidden-merge distinction the family protects. Claim ceiling is associational and explicitly disclaims causal or unique-mechanism inference. Confounders (session/subject/lab/movement/reaction time) are addressed via nuisance covariates, grouped nested resampling, and positive/negative controls including label-permutation and non-causal timing checks. Dataset evidence (ephys and behavior schemas, coverage counts) grounds the described joins, spike-shard reconstruction, and covariate availability without inventing facts. The single Owner-identified issue (locking eligibility thresholds, timing window, interaction basis, and capacity-matching specification before execution) is correctly a pre-execution lock: these are bounded implementation parameters that do not undermine the credibility of the planning-stage design. No scientific blocker or hard-boundary issue is present at round 0.

**Per criterion:**

- *intent preservation* — **pass**. The plan preserves the protected functional-versus-representational distinction, separating held-out behavioral prediction from cross-condition readout and avoiding classification of mixing type.
- *scientific value* — **pass**. A conditional, matched comparison of representation families with separate behavioral and generalization endpoints addresses a genuine open question about the functional consequences of mixed selectivity beyond mere prevalence.
- *dataset grounding* — **pass**. Required variables, joins (eid, trial_id, pid, cluster_id), spike-shard reconstruction needs, and coverage counts are consistent with the cited schema and coverage evidence views.
- *overclaim control* — **pass**. Claim ceiling is explicitly associational; interpretation_limits correctly disclaim causal and unique-mechanism claims and scope conclusions to quality-screened populations.
- *competing explanations* — **pass**. Alternative explanations (capacity mismatch, residual task/movement signal, hierarchical confounds, condition-difficulty asymmetry) are enumerated and addressed by matching and nuisance controls.
- *controls and validation* — **pass**. Positive controls (recoverable task signal, baseline choice prediction), negative controls (label permutation, non-causal shifted window), and a validation strategy for spike-shard decoding and fold-wise checks are specified.
- *material revision risk* — **not_applicable**. This is review_round 0 with no prior revision history; no material revision assessment applies yet.
- *sibling separation* — **pass**. The target_contrast explicitly distinguishes this variant from the representational-taxonomy sibling by focusing on conditional behavioral-prediction and readout outcomes rather than classifying mixing form.
- *prior issue resolution* — **not_applicable**. No prior review rounds exist; the issue ledger is empty and this is the first review cycle.

## Population geometry of brain-wide sensorimotor transformation

- **Question Owner** — Accepted (`openai` / `gpt-5.6-terra`)
- **Independent reviewer** — Accepted (`anthropic` / `claude-sonnet-5`)

**The reviewer's reasoning.** The revised plan directly resolves the round-0 scientific blocker: the primary estimand now restricts every baseline covariate (target-state history, task/contrast indicators, prior-trial history, wheel/DLC/movement-state features) to information available at or before the source bin, and explicitly relegates outcome-realized choice, reaction-time, response, and post-source movement adjustments to secondary descriptive analyses that cannot override the primary temporal-ordering claim. This preserves the intended predictive, non-causal contrast between temporally ordered cross-region prediction, independent local representation, and measured common-input accounts, with an appropriate predictive claim ceiling, explicit interpretation limits against communication/causal-routing overclaim, and both positive and negative controls (time-shift/permutation, non-preparation target window, event-alignment recovery). The two remaining Owner issues concern bounded pre-execution parameter choices (inclusion thresholds, bin/lag/window specification, missing-data tiers) that must be fixed before running the analysis but do not themselves threaten the validity of the current planning product; they are pre-execution locks, not scientific blockers, and are carried forward rather than requiring another revision round.

**Per criterion:**

- *intent preservation* — **pass**. The plan preserves the invariant's discriminating observation (incremental predictive contribution of an earlier source-region state to a later target-preparation state) and does not merge geometric correspondence with predictive dependence, consistent with the family's forbidden-merge constraint.
- *scientific value* — **pass**. The design contrasts a causal-past-only baseline against a matched model augmented with source-region state, aggregated hierarchically across sessions/pairs, which can meaningfully discriminate routing-like prediction from independent-local or common-input accounts.
- *dataset grounding* — **pass**. Schema and metadata evidence support the availability of region-labeled units, spike stores, and behavioral/wheel/DLC products keyed by eid/trial_id sufficient to construct the proposed population states and causal-past covariates, with coverage caveats appropriately flagged as limitations.
- *overclaim* — **pass**. Claim ceiling is explicitly 'predictive,' and interpretation limits state clearly that incremental prediction is not evidence of physical communication or causal routing, avoiding conflation of prediction with mechanism.
- *alternative explanations* — **pass**. Shared inputs, event-locking artifacts, unequal coverage, and unmeasured behavioral/internal-state variables are enumerated and addressed through causal-past restriction, sensitivity thresholds, and secondary descriptive adjustment.
- *controls* — **pass**. Negative controls (time-shifted/permuted source states, temporally invalid future source bins, non-preparation target windows) and positive controls (task-event recovery, target-history baseline improvement) are specified without conditioning inclusion on target performance.
- *material revision* — **pass**. The revision corrects covariate timing to align with the already-stated predictive, non-causal estimand; it does not alter the central phenomenon, target contrast, claim ceiling, or population scope, consistent with the Owner's material_revision_detected=false.
- *sibling separation* — **pass**. The variant remains confined to temporally ordered predictive dependence and does not encroach on the sibling geometry-invariance variant's cross-region geometric-correspondence question.
- *prior issue resolution* — **pass**. The sole prior scientific blocker (post-bin covariates contaminating the primary temporally ordered estimand) is resolved by restricting the primary baseline to causal-past information and moving outcome-realized adjustments to explicitly secondary descriptive analyses.
