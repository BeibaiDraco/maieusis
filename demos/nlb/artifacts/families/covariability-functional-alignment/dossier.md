> ⚠️ Provisional inspiration: planning continued from source-bound but not independently reviewed inputs. Dataset claims remain conditional or unverified, and this dossier cannot be elevated above provisional authority without independent review.

> ⚠️ Data basis: this family's open gap / key claims rest on ABSTRACT-ONLY literature (full text was not available to this system). The questions are literature-motivated but NOT fulltext-verified; a domain expert should confirm against primary sources.

# Functional meaning of motor-population co-variability

This is a development planning dossier. It is planning-only and does not report scientific results.
Dataset grounding level: `sample_inspected`.
Dataset claim status: `unverified`.

## Question Family

Tests whether the consequences of neural co-variability depend on its alignment with movement-relevant or context-relevant dimensions rather than on aggregate variability magnitude.

## Scientific Motivation

Changes in overall population variability may be descriptively prominent but functionally uninformative if only particular covariance components align with task-relevant geometry.

## Dataset Leverage

Dataset claim status is unverified; observed-depth label is sample_inspected. Dataset leverage remains a planning hypothesis, by variant: Variant 1: Repeated population activity paired with hand, cursor, and velocity measurements may allow later planning of movement-relevant alignment comparisons. Variant 2: If trial-level straight and curved contexts, behavioral trajectories, and repeated population observations are available, they may support comparison of independently specified context-demand dimensions against generic reference dimensions spanning matched temporal trajectory structure, kinematics, and muscle-related structure where relevant measurements permit. No scientific-result generation was performed.

## Variant Plans

- Variant 1 (Movement-alignment branch): Does the predictive consequence of motor-population co-variability depend more on alignment with movement-relevant dimensions than on its overall magnitude?
  - Distinguishing test: This variant defines functional relevance by alignment with continuous movement representation; the sibling defines it by alignment with trajectory-context distinctions. The discriminating observation is Movement prediction varies with the orientation of co-variability relative to independently characterized movement-relevant structure after accounting for aggregate magnitude.
  - Planned analysis: Feasible within-session: 142-unit M1+PMd co-variability tested for alignment with an independently characterized movement-representation subspace (from 1 kHz hand kinematics) versus aggregate magnitude, with cross-validated subspace/covariance estimation and reliability checks. Claim ceiling within-subject descriptive/associational.
    - Refined question: In delayed maze reaching, does the predictive consequence of motor-population co-variability depend more on its alignment with an independently characterized movement-representation subspace than on its aggregate magnitude?
    - Population and scope: Full simultaneously recorded M1+PMd population (142 sorted units: 72 PMd, 70 M1) from single subject sub-Jenkins, single session (100 train trials; 75 train / 25 val split column). Inference is within-subject/within-session; region-stratified M1-vs-PMd analyses are sensitivity checks per the Owner ruling. No cross-subject/cross-session/causal claims.
    - Data sources: Sorted-unit spike times (units group) for the 142-unit M1+PMd population used to estimate population co-variability.; Expected grain: One ragged spike-time series per unit with per-unit observation intervals; binned to per-trial population spike-count vectors.; Limitations: ~100 trials and 142 units make high-dimensional covariance sampling-limited; shrinkage and cross-validation required. Continuous 1 kHz hand position and velocity (processing/behavior) used to characterize an independent movement-representation reference subspace.; Expected grain: 2D hand position/velocity time series at ~1 ms spacing, windowed to trial movement epochs.; Limitations: 2D hand/cursor kinematics only; no joint or limb-configuration signals. Trial timing and condition metadata (intervals/trials) anchoring movement epochs and conditions.; Expected grain: One row per trial with move_onset_time, go_cue_time, maze_id, num_barriers.; Limitations: Small per-condition trial counts constrain condition-resolved estimates.
    - Unit of observation: Per-trial binned population spike-count vector within the movement epoch (aligned to move onset).
    - Unit of inference: Trial (and condition) within the single session; dependence handled by cross-validation and condition-aware resampling.
    - Hierarchy and dependence: Trials are nested within maze layouts/conditions with repeated observations; covariance and subspace estimation use disjoint trial folds, and reliability is assessed with condition-aware split-half and bootstrap resampling.
    - Analysis strategy: Bin spike counts within a prespecified movement epoch and build per-trial population activity; remove condition means to obtain trial-to-trial residuals and also retain across-condition structure. On held-out folds, characterize an independent movement-representation subspace by mapping population activity to hand position/velocity (e.g., cross-validated reduced-rank/regression or kinematic-PSTH PCA), never using the covariance to be tested. Estimate population co-variability with a shrinkage covariance estimator; decompose it into components aligned with versus orthogonal to the movement subspace and compute an alignment index. Assess the predictive consequence by cross-validated prediction of hand kinematics from population activity, testing whether variance aligned with movement dimensions carries predictive consequence beyond aggregate covariance magnitude entered as a competing predictor.
    - Candidate estimands: Population alignment index of co-variability with the movement-representation subspace. Partial association between movement-aligned (vs total) covariance and cross-validated movement prediction, controlling for aggregate covariance magnitude.
    - Validation strategy: Synthetic method-recovery on populations with known aligned-versus-magnitude structure to confirm the alignment estimator and the magnitude control before touching data; cross-validated subspace estimation; split-half covariance reliability gating.
    - Split strategy: Leakage-safe nested cross-validation: movement subspace and covariance estimated on disjoint trial folds; the benchmark test file (no behavior) is never used for behavioral outcomes.
    - Diagnostics: Split-half and bootstrap covariance reliability; eigenspectrum/dimensionality; firing-rate stability and unit inclusion; region (M1 vs PMd) composition of aligned components.
    - Negative controls: Alignment of co-variability with a movement-irrelevant or random subspace; trial-label-shuffled covariance.
    - Positive controls: Recovery of known velocity-tuned/movement-encoding directions that the alignment method should detect.
    - Alternative explanations: Aggregate variability magnitude alone explains predictive differences. Estimated alignment reflects limited sampling or dimensionality-estimation error. Movement-aligned covariance is inherited from kinematic heterogeneity rather than neural organization.
    - Predicted result patterns: Movement-aligned covariance predicts movement beyond aggregate magnitude, supporting a structural-alignment account. Aggregate magnitude dominates and alignment adds nothing, supporting a magnitude account.
    - Claim ceiling: associational (preserved modes: descriptive, associational)
    - Interpretation limits: Single subject/single session; ~100 trials; no cross-subject or causal generalization. Cross-validated prediction is an analysis method, not a causal claim about co-variability. Planning evidence is not a scientific result.
    - Resource estimate: Bounded: spike binning, cross-validated subspace/regression, shrinkage covariance, and resampling reliability over 142 units x ~100 trials; modest single-machine compute.
    - Why this serves the question: It preserves the movement-representation alignment contrast, pits structural alignment against aggregate magnitude with an independent reference and circularity controls, and reports honestly within the dataset's within-session reliability limits.
    - Unresolved planning decisions: Bin width, movement-epoch window, shrinkage estimator, and alignment-index definition to be prespecified before execution.
  - What would support or weaken it: A positive result would support structural alignment as more informative than global variability magnitude. A negative result would favor magnitude-based or alternative non-alignment accounts. A null result would leave open whether alignment is irrelevant or too uncertain to estimate in this setting.
- Variant 2 (Residual context-alignment branch): After accounting for matched generic temporal, kinematic, and, where relevant, muscle-related structure, does motor-population co-variability retain selective alignment with independently defined dimensions of straight-versus-curved reach demands and improve prediction of reach context?
  - Distinguishing test: Unlike the movement-alignment sibling, this variant requires residual alignment with independently defined straight-versus-curved trajectory demands and incremental context prediction after generic temporal, kinematic, magnitude, and relevant muscle-related structure is accounted for. The discriminating observation is Context dimensions defined independently of the neural covariance data from obstacle or trajectory requirements—not by overall movement magnitude—show reliable residual alignment with co-variability and improve straight-versus-curved context prediction after variance attributable to matched temporal trajectories, kinematics, aggregate covariance magnitude, and, where relevant, muscle-related reference dimensions is accounted for. Conversely, a reliably estimated covariance structure that remains shared across contexts and is captured by the generic reference space, without residual context alignment or prediction improvement, favors a preserved-manifold account; insufficient reliability makes that contrast inconclusive.
  - Planned analysis: Feasible within-session with one documented gap: straight-versus-curved context and barrier/trajectory geometry define context dimensions independently of neural data; matched temporal + kinematic + magnitude reference modes can be residualized; residual context alignment and incremental context prediction are testable. The muscle-related comparator is unavailable (no EMG) and is omitted per the invariant's 'where relevant' scoping. Requires implementing a residualization-plus-reliability-gated context-alignment executor skill. Claim ceiling within-subject descriptive/associational.
    - Refined question: After accounting for matched generic temporal, kinematic, and aggregate-magnitude structure, does M1+PMd co-variability retain reliable residual alignment with independently defined straight-versus-curved trajectory demands and improve prediction of reach context?
    - Population and scope: Full simultaneously recorded M1+PMd population (142 units) from single subject sub-Jenkins, single session; straight (num_barriers=0, 32 trials) versus curved (num_barriers=9, 68 trials) reaches across 9 maze layouts. Within-subject/within-session inference with region-stratified sensitivity; no cross-subject/cross-session/causal claims.
    - Data sources: Sorted-unit spike times for the 142-unit M1+PMd population used to estimate co-variability and residual covariance after generic-mode removal.; Expected grain: Per-unit spike times binned to per-trial population spike-count vectors.; Limitations: Small per-condition counts strongly limit residual-covariance reliability; reliability gating required. Trial context and geometry (num_barriers, maze_id, barrier_pos, target_pos) defining straight-versus-curved trajectory demands independently of the neural covariance.; Expected grain: One row per trial plus per-trial barrier/target geometry vectors.; Limitations: Curvature/demand labels must be derived from geometry/kinematics, not from neural data, to avoid circularity. Continuous 1 kHz hand kinematics and trial timing providing matched generic temporal and kinematic reference modes to residualize before testing residual context alignment.; Expected grain: 2D hand position/velocity time series windowed to trial epochs, plus per-trial temporal anchors.; Limitations: No EMG in this release; the muscle-related reference dimension is unavailable and omitted (permitted 'where relevant'), leaving muscle confounds controllable only indirectly via kinematics.
    - Unit of observation: Per-trial binned population spike-count vector within matched temporal windows, labeled by independently defined straight-versus-curved context.
    - Unit of inference: Trial (and maze condition) within the single session; dependence handled by condition-aware cross-validation and permutation.
    - Hierarchy and dependence: Trials nested within maze layouts and context; matched temporal/kinematic reference is fit and residualized on disjoint folds, and context prediction is cross-validated across trials with condition-aware splits to prevent layout leakage.
    - Analysis strategy: Define straight-versus-curved context and trajectory-demand dimensions from num_barriers, maze layout, and barrier/target geometry (and kinematic curvature), independently of the neural covariance. Construct a matched generic reference space from temporal (time-in-trial) and kinematic (hand position/velocity) structure and aggregate covariance magnitude, matched across contexts; regress this reference out of population activity/covariance. Test whether residual co-variability retains reliable alignment with the independent context dimensions and whether residual population activity improves cross-validated straight-versus-curved context classification beyond the generic reference and magnitude. Gate all conclusions on covariance/alignment reliability: if residual covariance cannot be estimated reliably, report inconclusive rather than a preserved-manifold null.
    - Candidate estimands: Residual alignment index between context dimensions and co-variability after generic-mode and magnitude removal. Incremental cross-validated context-prediction performance from residual population structure beyond the generic reference space and aggregate magnitude.
    - Validation strategy: Synthetic method-recovery distinguishing a genuinely shared (preserved-manifold) covariance from an unreliably estimated context-selective covariance, to calibrate the reliability threshold and confirm the residualization removes only generic structure; permutation of context labels as a null.
    - Split strategy: Leakage-safe condition-aware cross-validation: reference fitting, residualization, and context decoding on disjoint trial/layout folds; benchmark test outcomes never inspected.
    - Diagnostics: Split-half/bootstrap reliability of residual covariance; balance/matching quality of the generic reference across contexts; straight-vs-curved trial-count imbalance handling; region composition of residual context-aligned structure.
    - Negative controls: Context relabeled by a random split matched on movement magnitude; kinematic-only proxy standing in for the absent muscle reference.
    - Positive controls: A synthetic residual context mode injected into the population that the pipeline should recover after generic-mode removal.
    - Alternative explanations: Apparent context alignment attributable to generic temporal trajectory structure, kinematics, movement magnitude, or (unmeasured) muscle-related dimensions. Covariance modes are reliably preserved across straight and curved reaches with no residual context organization. Apparent selectivity from circular or post hoc definition of context dimensions using the neural covariance. Failure to detect residual selectivity due to unreliable covariance/alignment estimation rather than genuinely shared modes.
    - Predicted result patterns: Reliable residual alignment and incremental context prediction beyond generic modes and magnitude, supporting context-selective organization. Reliably estimated covariance that remains shared across contexts and is captured by the generic reference, with no residual alignment or prediction gain, supporting a preserved-manifold account.
    - Claim ceiling: associational (preserved modes: descriptive, associational)
    - Interpretation limits: No EMG: muscle-mapping confound controlled only indirectly via kinematics; this is a documented limitation, not a resolved control. Small per-condition counts and a single session cap reliability; an inconclusive outcome is a genuine possibility distinct from a preserved-manifold null. Within-subject descriptive/associational only; context dimensions defined independently of neural covariance; planning evidence is not a scientific result.
    - Resource estimate: Bounded: reference construction, residualization, cross-validated context decoding, and reliability/permutation resampling over 142 units x ~100 trials; modest single-machine compute.
    - Why this serves the question: It preserves the residual context-selective alignment contrast, separates it from generic temporal/kinematic/magnitude structure, defines context independently of neural data, and honestly gates on reliability, while documenting the unavailable muscle comparator as a limitation rather than silently dropping the control.
    - Required new skills: A residualization-plus-reliability-gated context-alignment executor skill that removes a matched generic reference space, estimates residual covariance alignment with independently defined context dimensions, and reports inconclusive when covariance reliability is unmet.
    - Unresolved planning decisions: Exact definition of the straight-versus-curved demand dimensions and the matched reference basis, and the reliability threshold for declaring an inconclusive result, to be prespecified before execution.
  - What would support or weaken it: Reliable residual alignment and incremental context prediction would support the interpretation that motor-population co-variability contains organization selective for straight-versus-curved trajectory demands beyond shared movement-related covariance. If covariance modes are estimated reliably, remain shared across straight and curved reaches, and are explained by the specified generic reference space without residual context alignment or prediction improvement, the result would favor a generic preserved-manifold account over context-selective organization. If covariance orientation, shared-mode preservation, or residual alignment cannot be estimated reliably, the result would not distinguish a preserved-manifold account from undetected context-selective covariance.

## Competing Explanations And Controls

Competing explanations that must remain visible, by variant: Variant 1: Aggregate variability magnitude alone explains predictive differences. Estimated alignment reflects limited sampling or dimensionality-estimation error. Movement-aligned covariance is inherited from kinematic heterogeneity rather than neural organization. Variant 2: Apparent context alignment is attributable to generic temporal trajectory structure, kinematics, movement magnitude, or muscle-related dimensions rather than contextual demands. Covariance modes are reliably preserved across straight and curved reaches, with no residual context-selective organization. Apparent selectivity arises from circular or post hoc definition of context dimensions using the same neural covariance data. Failure to detect residual selectivity reflects unreliable covariance or alignment estimation rather than genuinely shared covariance modes.

## Outcome Meanings

Possible outcome meanings, by variant: Variant 1: A positive result would support structural alignment as more informative than global variability magnitude. A negative result would favor magnitude-based or alternative non-alignment accounts. A null result would leave open whether alignment is irrelevant or too uncertain to estimate in this setting. Variant 2: Reliable residual alignment and incremental context prediction would support the interpretation that motor-population co-variability contains organization selective for straight-versus-curved trajectory demands beyond shared movement-related covariance. If covariance modes are estimated reliably, remain shared across straight and curved reaches, and are explained by the specified generic reference space without residual context alignment or prediction improvement, the result would favor a generic preserved-manifold account over context-selective organization. If covariance orientation, shared-mode preservation, or residual alignment cannot be estimated reliably, the result would not distinguish a preserved-manifold account from undetected context-selective covariance.

## Planning Status and Limits

This accepted plan is a completed planning outcome under automated independent review. Optional post-hoc human review may be imported later. It remains planning-only and does not authorize a downstream bridge or execution or claim scientific results.

- Dataset statements are labeled `unverified`; planner-authored locators or digests alone do not verify an observation.
- Dataset grounding here reached bounded inspection of dataset samples; it includes no scientific-result values or execution outputs.
- A separate bridge approval is still required before downstream execution artifacts.
